import './styles.scss';

const Loading = () => {
  return (
    <main className='loading'>
      <div className='loading-spinner'></div>
      <span>Loading ...</span>
    </main>
  );
};

export default Loading;
